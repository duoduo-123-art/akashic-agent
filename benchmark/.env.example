# Akasic Benchmark 环境变量示例
# benchmark 现在默认从项目根目录 config.json 读取模型、端点、embedding、检索参数。

# 仅当 config.json 中使用 ${ENV_VAR} 占位符时，才需要在这里提供对应环境变量。

# OPENAI_API_KEY=sk-xxxx
# DASHSCOPE_API_KEY=sk-xxxx
# DEEPSEEK_API_KEY=sk-xxxx
